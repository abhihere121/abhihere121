databases:
  - name: sizesignal-db
    databaseName: sizesignal
    user: sizesignal
    plan: starter

services:
  - type: web
    name: sizesignal
    env: docker
    plan: starter
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sizesignal-db
          property: connectionString
      - key: SHOPIFY_API_KEY
        sync: false
      - key: SHOPIFY_API_SECRET
        sync: false
      - key: APP_ENCRYPTION_KEY
        sync: false
      - key: EMBED_SIG_SECRET
        sync: false
      - key: SHOPIFY_SCOPES
        value: read_products,read_inventory,write_script_tags,write_webhooks
      - key: ALLOW_DEV_SEED
        value: "false"
